{
  "buildCommand": "cd OkapiFind && rm -rf .expo web-build node_modules/.cache && npm install --legacy-peer-deps && npx expo export:web",
  "outputDirectory": "OkapiFind/web-build",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
