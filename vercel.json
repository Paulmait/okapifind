{
  "buildCommand": "cd OkapiFind && npm install --legacy-peer-deps && npx expo export --platform web",
  "outputDirectory": "OkapiFind/dist",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
